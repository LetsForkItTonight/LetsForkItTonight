# Program to calculate average of numbers

numbers = [10, 20, 30, 40, 50]

total = 0

for i in range(0, len(numbers)):
    total = total + numbers[i]

average = total / len(number)   # BUG 1: Wrong variable name

print("Total is: " + total)     # BUG 2: Can't add int to string

if average > 25
    print("Above Average")      # BUG 3: Missing colon

else
    print("Below Average")      # BUG 4: Missing colon
